{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "echo \"static deploy — no build (Vite build is switched on when the sim becomes the client)\"",
  "outputDirectory": ".",
  "cleanUrls": true
}
